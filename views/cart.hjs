<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart - GIU Food Truck</title>
    <link rel='stylesheet' href='/styles/bootstrap.min.css' />
    <link rel='stylesheet' href='/styles/style.css' />
    <script src="/js/jquery-2.2.0.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.min.js" type="text/javascript"></script>
    <script src="/src/cart.js" type="text/javascript"></script>
  </head>
  <body>
    <nav class="navbar navbar-inverse navbar-custom">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/dashboard">GIU Food Truck</a>
        </div>
        <ul class="nav navbar-nav">
          <li><a href="/dashboard">Dashboard</a></li>
          <li><a href="/trucks">Browse Trucks</a></li>
          <li class="active"><a href="/cart">Cart</a></li>
          <li><a href="/myOrders">My Orders</a></li>
          <li><a href="#" id="logoutBtn">Logout</a></li>
        </ul>
      </div>
    </nav>

    <div class="container">
      <div class="text-center mb-3">
        <h1>Shopping Cart</h1>
        <p>Review your items and place your order</p>
      </div>

      <div id="alertContainer"></div>

      <div id="cartContainer">
        <div class="card">
          <div class="card-header">Cart Items</div>
          <div class="card-body">
            <div id="cartItemsTable" style="overflow-x: auto;">
              <table class="table">
                <thead>
                  <tr>
                    <th>Item Name</th>
                    <th>Price</th>
                    <th>Quantity</th>
                    <th>Subtotal</th>
                    <th>Actions</th>
                  </tr>
                </thead>
                <tbody id="cartItemsBody">
                  <!-- Cart items will be loaded here -->
                </tbody>
                <tfoot>
                  <tr>
                    <td colspan="3" style="text-align: right; font-weight: bold;">Total:</td>
                    <td id="cartTotal" style="font-weight: bold; font-size: 1.2em;">$0.00</td>
                    <td></td>
                  </tr>
                </tfoot>
              </table>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="card-header">Place Order</div>
          <div class="card-body">
            <div class="form-group">
              <label for="scheduledPickupTime" class="form-label">Scheduled Pickup Time</label>
              <input type="datetime-local" class="form-control" id="scheduledPickupTime" name="scheduledPickupTime" required>
              <small style="color: #000000; display: block; margin-top: 5px;">Please select when you would like to pick up your order</small>
            </div>
            <div style="text-align: center; margin-top: 20px;">
              <button type="button" id="placeOrderBtn" class="btn-primary" style="padding: 12px 40px; font-size: 18px;">
                Place Order
              </button>
            </div>
          </div>
        </div>
      </div>

      <div id="emptyState" class="empty-state" style="display: none;">
        <h3>Your Cart is Empty</h3>
        <p>Add some delicious items from our food trucks to get started!</p>
        <a href="/trucks" class="btn-primary" style="display: inline-block; margin-top: 15px; text-decoration: none;">
          Browse Trucks
        </a>
      </div>
    </div>

    <script>
      // Logout functionality
      $(document).ready(function() {
        $('#logoutBtn').click(function(e) {
          e.preventDefault();
          document.cookie = 'session_token=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';
          location.href = '/';
        });
      });
    </script>
  </body>
</html>

